config {
  type: "table",
  schema: "dataform",
  name: "page_views_summary"
}

SELECT
  user_pseudo_id,
  COUNT(*) AS total_page_views
FROM `lively-antonym-388912.analytics_477969413.events_*`
WHERE event_name = 'page_view'
GROUP BY user_pseudo_id
ORDER BY total_page_views DESC;
